query GetMaintenanceMode {
  runtime {
    systemTenantModeConfiguration(first: 1) {
      edges {
        node {
          rtId
          maintenanceLevel
          environmentMode
        }
      }
    }
  }
}
